#include <bits/stdc++.h>
using namespace std;

int32_t main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int l, r;
    cin>> l >> r;

    cout << abs(l-r) << '\n';

    return 0;
}